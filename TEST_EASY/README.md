# 🎨 TEST_EASY - Спрощена версія SD Inference Server

## 📋 Опис

**TEST_EASY** - це спрощена версія sd-inference-server для людей, які хочуть використовувати Stable Diffusion без зайвої складності.

### ✨ Основні можливості

- 🖼️ **txt2img** - Генерація зображень з текстового опису
- 📸 **img2img** - Модифікація існуючих зображень
- 🎭 **inpaint** - Видалення/редагування части зображення за допомогою маски
- 🎯 **LoRA підтримка** - Використання LoRA моделей для розширення функціоналу
- 🏗️ **Checkpoint підтримка** - Вибір різних моделей Stable Diffusion
- 🚀 **Upscaler підтримка** - Збільшення розміру зображень
- ⚙️ **Гнучкі параметри** - Редагування всіх важливих параметрів

## 🚀 Швидкий старт

### Google Colab (Сервер)

1. **Відкрийте Google Colab notebook** (скоро завантажимо)
2. **Запустіть клітинки** - сервер буде готовий за ~5 хвилин
3. **Отримайте URL** - буде показано посилання для клієнта

### Python Клієнт (Локально)

```bash
# 1. Встановіть залежності
pip install -r requirements.txt

# 2. Налаштуйте URL сервера в config.json
{
    "server_url": "http://your-colab-link.ngrok.io",
    "timeout": 300
}

# 3. Запустіть клієнт
python client_gui.py
```

### EXE Клієнт (Windows)

1. Завантажте `client.exe`
2. Запустіть його
3. Введіть URL сервера
4. Почніть генерувати!

## 📁 Структура проекту

```
TEST_EASY/
├── server/
│   ├── colab_server.ipynb          ← Google Colab notebook
│   ├── easy_wrapper.py             ← Спрощена версія wrapper
│   ├── easy_models.py              ← Управління моделями
│   └── easy_inference.py           ← Спрощена інференція
│
├── client/
│   ├── client_gui.py               ← GUI клієнт з маскою
│   ├── client_cli.py               ← CLI клієнт
│   ├── mask_editor.py              ← Редактор масок для inpaint
│   └── config.json                 ← Конфіг клієнта
│
├── docs/
│   ├── SETUP.md                    ← Встановлення
│   ├── USAGE.md                    ← Використання
│   ├── API.md                      ← API документація
│   └── TROUBLESHOOTING.md          ← Розв'язання проблем
│
├── examples/
│   ├── example_txt2img.py
│   ├── example_img2img.py
│   ├── example_inpaint.py
│   └── batch_processing.py
│
└── README.md                        ← Цей файл
```

## 🎯 Потоки роботи

### txt2img (Текст → Зображення)

```
Користувач вводить промпт
    ↓
Вибирає Checkpoint
    ↓
Додає LoRA (опціонально)
    ↓
Налаштовує параметри (розмір, кроки, scale)
    ↓
Нажимає "Generate"
    ↓
Результат з'являється на екрані ✅
```

### img2img (Зображення → Зображення)

```
Користувач завантажує зображення
    ↓
Вводить промпт
    ↓
Встановлює Strength (0.0-1.0)
    ↓
Вибирає Checkpoint та LoRA
    ↓
Генерує нове зображення ✅
```

### inpaint (Редагування через маску)

```
Користувач завантажує зображення
    ↓
Малює маску (у вбудованому редакторі)
    ↓
Вводить промпт для змін
    ↓
Генерує результат ✅
```

## 🔧 Параметри, які можна редагувати

### Базові параметри
- **Checkpoint** - Вибір моделі (SDv1.5, SDXL, тощо)
- **Prompt** - Опис зображення
- **Negative Prompt** - Що не робити

### Розмір та якість
- **Width** - Ширина (512, 768, 1024)
- **Height** - Висота (512, 768, 1024)
- **Steps** - Кількість кроків (20-100)
- **Scale** - Сила впливу промпта (7.5-15)

### Додатково
- **LoRA** - Використовуване LoRA
- **Upscaler** - Збільшення розміру
- **CLIP Skip** - Пропуск шарів CLIP (1-2)
- **Seed** - Насіння для повторюваності (-1 = випадкове)

## 📊 Системні вимоги

### Google Colab (Сервер)
- ✅ Вільна версія Colab достатня
- ✅ GPU буде автоматично
- ✅ Немає потреби в локальному GPU

### Локальний клієнт (Python)
- **Python 3.8+**
- **pip** або **conda**
- Залежності: `requests`, `pillow`, `PyQt5` (для GUI)

### EXE клієнт
- **Windows 7+**
- **Інтернет з'єднання**
- Все вбудовано!

## 🌐 API

Сервер слухає на порту 8000 та сприймає JSON запити:

```python
# txt2img
POST /txt2img
{
    "prompt": "a cat",
    "negative_prompt": "",
    "checkpoint": "model.safetensors",
    "width": 512,
    "height": 512,
    "steps": 20,
    "scale": 7.5,
    "seed": -1,
    "lora": "lora_name.safetensors"
}

# img2img
POST /img2img
{
    ... як txt2img +
    "image": "base64_encoded_image",
    "strength": 0.75
}

# inpaint
POST /inpaint
{
    ... як txt2img +
    "image": "base64_encoded_image",
    "mask": "base64_encoded_mask"
}
```

## 🎓 Приклади використання

### Python клієнт
```python
from client import EasyClient

client = EasyClient("http://localhost:8000")

# txt2img
result = client.txt2img(
    prompt="beautiful landscape",
    width=768,
    height=768
)
result.save("output.png")

# img2img
result = client.img2img(
    prompt="turn it to oil painting",
    image="input.png",
    strength=0.7
)
result.save("output.png")
```

### CLI команди
```bash
# txt2img
python client_cli.py txt2img --prompt "a cat" --width 512

# img2img
python client_cli.py img2img --prompt "oil painting" --image input.png

# inpaint
python client_cli.py inpaint --prompt "add flowers" --image input.png --mask mask.png
```

## 📚 Документація

- 📄 [SETUP.md](docs/SETUP.md) - Детальне встановлення
- 📄 [USAGE.md](docs/USAGE.md) - Як користувати
- 📄 [API.md](docs/API.md) - API документація
- 📄 [TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md) - Розв'язання проблем

## 🐛 Розв'язання проблем

### Сервер не запускається
```
Рішення: Перевірте GPU доступність на Colab
```

### Клієнт не може підключитися
```
Рішення: Перевірте URL та інтернет з'єднання
```

### Маска не працює в inpaint
```
Рішення: Переконайтеся, що маска має той же розмір що і зображення
```

## 🤝 Спільнота

Знайшли баг? Маєте ідеї? Залишайте issues!

## 📄 Ліцензія

Той же як оригінальний sd-inference-server

## 🎉 Що дальше?

- [ ] Batch processing (декілька зображень)
- [ ] ControlNet підтримка
- [ ] Inpainting з автоматичним знаходженням об'єктів
- [ ] Web інтерфейс
- [ ] Mobile app

---

**Версія:** 1.0.0  
**Дата:** 21 листопада 2024  
**Статус:** Beta (в розробці) 🚀

Починаємо! 🎨
